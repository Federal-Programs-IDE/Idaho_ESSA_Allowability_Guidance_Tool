A search tool users can access on a web browser to receive guidance on what expenses are allowable, as well as relevant information

Initially developed in python, then converted to HTML.  'embeddings' is from an LLM that translates user entries and connects to the 'allowable_use_mapping.csv" to provide guidance.
